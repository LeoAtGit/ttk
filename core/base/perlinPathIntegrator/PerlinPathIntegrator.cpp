#include <PerlinPathIntegrator.h>

ttk::PerlinPathIntegrator::PerlinPathIntegrator() {
  // inherited from Debug: prefix will be printed at the beginning of every msg
  this->setDebugMsgPrefix("PerlinPathIntegrator");
}
